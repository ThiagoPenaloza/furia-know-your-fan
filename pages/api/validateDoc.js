// import { validateDocument } from '../../utils/api';

// export default async function handler(req, res) {
//   const { text } = req.body;
//   try {
//     const result = await validateDocument(text);
//     res.status(200).json(result);
//   } catch(e) {
//     res.status(500).json({ error: e.message });
//   }
// }